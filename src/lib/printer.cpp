#include <ostream>

#include "printer.h"

Printer::Printer() {

};

  // Printer(Code pCode) : code(pCode) {}
  // friend std::ostream&
  // operator<<(std::ostream& os, const Modifier& mod) {
  //   return os << "\033[" << mod.code << "m";
  // }
